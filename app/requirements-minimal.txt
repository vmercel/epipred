# Minimal requirements for deployment to avoid compilation issues
Flask==2.3.3
Werkzeug==2.3.7
numpy==1.24.3
pandas==2.0.3
gunicorn==21.2.0

# Use CPU-only TensorFlow to reduce size and avoid GPU dependencies
tensorflow-cpu==2.13.1

# Skip scikit-learn to avoid Cython compilation issues
# matplotlib and other visualization libraries are optional for core functionality
